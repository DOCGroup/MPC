
project {
  Define_Custom(SWIG) {
    automatic         = 1
    command           = swig -perl -c++
    output_option     = -o
    inputext          = .swg, .swig, .i
    source_outputext  = _wrap.cpp
    generic_outputext = .pm
  }
}
