// $Id$
project {
  Define_Custom(YACC) {
    automatic        = 1
    command          = yacc
    commandflags     = -d
    postcommand      = <%mv%> y.tab<%output_ext%> <%output%>
    pch_postrule     = 1
    inputext         = .yy, .y
    pre_extension    = .tab
    source_outputext = .c
    header_outputext = .h
  }
}
