// $Id$

project {
  requires += sqlite3

  includes += $(SQLITE_ROOT)/include
  libpaths += $(SQLITE_ROOT)/lib

  specific (vc6, vc7, vc71, vc8, vc9, nmake) {
    lit_libs += sqlite3
  } else {
    libs += sqlite3
  }
}
