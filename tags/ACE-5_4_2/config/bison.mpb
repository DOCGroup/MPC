// $Id$
project {
  Define_Custom(BISON) {
    automatic        = 1
    command          = bison
    commandflags     = -d
    pch_postrule     = 1
    inputext         = .y
    pre_extension    = .tab
    source_outputext = .c
    header_outputext = .h
  }
}
